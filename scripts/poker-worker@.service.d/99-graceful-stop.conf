# Unit-specific drop-ins load after generic service.d defaults. Fedora ships a
# generic TimeoutStopFailureMode=abort override, which could core-dump this
# many-GB solver when its drain window expires.
[Service]
TimeoutStopFailureMode=kill
