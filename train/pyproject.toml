# Value-net training harness (design doc 09, phase b). `uv sync` here, then
# `uv run train.py --data <corpus dir>` / `uv run eval.py`.
[project]
name = "valuenet-train"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["torch>=2.4", "numpy>=2"]
