# Target-specific rustflags fully REPLACE any global [build] rustflags (e.g. a
# machine-wide -Ctarget-cpu=native, which breaks wasm-bindgen's externref
# transform). The array must be non-empty or cargo ignores it.
[target.wasm32-unknown-unknown]
rustflags = ["-Ctarget-feature=+reference-types"]
